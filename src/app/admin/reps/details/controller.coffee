angular.module 'interreps'
 .controller 'AdminRepsDetailsController', ($rootScope, $scope, $timeout, $interval, $filter, $state, rep) ->
    'ngInject'

    # Definitions
    $scope.rep = rep

    # Methods

    # Linteners & Watchers


    return
